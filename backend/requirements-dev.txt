# Optional development / experiment dependencies (not needed to run the API or CI tests).
# Install: pip install -r requirements-dev.txt
# Lint/typecheck (also used in CI): pip install -r requirements-tools.txt
-r requirements.txt
langgraph>=0.2.60
requests>=2.32.0

# Coverage
pytest-cov==5.0.0

# Stress Testing
locust==2.29.0

# Security Auditing
bandit==1.7.9
pip-audit==2.7.3
