{
  "name": "astral-cassini",
  "private": true,
  "description": "Repository root marker (npm lockfile for tools that expect one at the Git root). The Next.js app lives in frontend/ — see README."
}
